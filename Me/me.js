function preload() {

}

function setup() {
    var canvas = createCanvas(windowWidth, windowHeight);
    canvas.parent("body")
    canvas.position(0,0)

    let div = meNavBar()
    div.position(0,0)

}

function draw() {
    // background(1,100,0,5)
    // circle(mouseX, mouseY, 10)
    
}

